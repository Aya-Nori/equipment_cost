<%# CSS・・・assets/stylesheets/list.css %>

<%# if some_condition %> <!-- 一定条件が満たされた場合現在使用中の機器を表示する -->
  <h3>現在使用中の機器</h3>
    使用中の機器があるとき、以下のように表示する。
    <div class='list-all'>

      <div class='list-nav'>
      <div class='list-info'>
        機器：<%= "機器名" %><br>
        利用者：<%= "利用者名" %><br>
        内線番号：<%= "内線番号" %><br>
      </div>
      </div>

      <div class='list-nav'>
      <div class='list-info'>
        機器：<%= "機器名" %><br>
        利用者：<%= "利用者名" %><br>
        内線番号：<%= "内線番号" %><br>
      </div>
      </div>

      <div class='list-nav'>
      <div class='list-info'>
        機器：<%= "機器名" %><br>
        利用者：<%= "利用者名" %><br>
        内線番号：<%= "内線番号" %><br>
      </div>
      </div>

    </div>
<%# else %>
  <br>
  <br>
  使用中の機器がないとき、以下のように表示する。
  <p>現在使用中の機器はありません。</p>
<%# end %>

<% (@equipments || []).group_by(&:category).each do |category, category_equipments| %>
  <div>
    <% category_equipments.each do |equipment| %>
      <% latest_start_finish_time = equipment.start_finish_times.order(created_at: :desc).first %>
      <div>
        <div>
          <% if latest_start_finish_time && latest_start_finish_time.condition == 1 %>
            <div class='user_now_on'>
              <p>現在、<%= latest_start_finish_time.user.name_sei %>（<%= latest_start_finish_time.user.extention_number %>）さんが利用中です。</p>
            </div>
          <% else %>
            <div class='user_now_off'>
              <p>現在、利用者はいません。</p>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
  <p>現在使用中の機器はありません。</p>
